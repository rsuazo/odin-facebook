<h1>User List</h1>

<div id="users">
<% for user in @users %>
      <% if !user.in?(current_user.friends) && user != current_user %>
        <div class="user">
          <p>
            <strong><%=h user.first_name %></strong>
            <%= link_to "Add Friend", friendships_path(:friend_id => user), :method => :post %>
            <div class="clear"></div>
          </p>
        </div>
      <% end %>
<% end %>
</div>